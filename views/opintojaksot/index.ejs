<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>Opintojaksot</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>
<body class="container" style="margin-top: 50px;">

    <% if (messages.success) { %>
    <div class="alert alert-success" role="alert"><%- messages.success %></div>
    <% } %>

    <% if (messages.error) { %>
    <div class="alert alert-danger" role="alert"><%- messages.error %></div>
    <% } %>

    <div class="card"> 
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-pill active">
                    <a class="nav-link">Opintojaksot</a>
                </li>
                <li class="nav-pill ml-auto">
                    <a class="nav-link active" href="/opintojaksot/add">Lisää opintojakso</a>
                </li>
            </ul>
        </div>
    <div class="card-body">
        
        <% if(data.length) { %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">#</th>
                    <th scope="col">Koodi</th>
                    <th scope="col">Laajuus</th>
                    <th scope="col">Nimi</th>
                    <th width="200px">Action</th>
                </tr>
            </thead>
            <tbody>
            <% for(var i = 0; i< data.length; i++) { %>
                <tr>
                    <th scope="row"><%= (i+1) %></th>
                    <td><%= data[i].Koodi%></td>
                    <td><%= data[i].Laajuus%></td>
                    <td><%= data[i].Nimi%></td>
                    <td>
                        <a class="btn btn-success edit" href="../opintojaksot/edit/<%=data[i].idOpintojakso%>">Muokkaa</a>                       
                        <a class="btn btn-danger delete" onclick="return alert('Oletko varma?')" href="../opintojaksot/delete/<%=data[i].idOpintojakso%>">Poista</a>                       
                    </td>
                </tr>
            <% } %>
            </tbody>
        </table>
        <% } %>
        
        <!-- if result is empty -->
        <% if(!data.length) { %>
            <p class="text-center">Opintojaksoa ei löydy!</p>
        <% } %>

        </div>
    </div>

</body>
</html>